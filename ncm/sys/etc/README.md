# Directory `ncm/sys/etc`

In this directory you will find php configuration files for XDebug and error reporting which will be used when creating a docker container with the given docker-compose file.
