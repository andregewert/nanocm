<?php
/* @var $this \Ubergeek\NanoCm\Module\AdminInstallationModule */

use Ubergeek\NanoCm\Util;

?>
    <table class="list">
        <thead>
            <tr>
                <th style="text-align: center" width="30"><input type="checkbox" class="selectall" /></th>
                <th>Datum des Backups</th>
                <th>Dateiname</th>
                <th class="textright">Dateigr&ouml;&szlig;e</th>
                <th class="textright">nanoCM-Version</th>
            </tr>
        </thead>
        <tbody>
            <?php if (is_array($this->existingBackups) && count($this->existingBackups) > 0) : ?>
                <?php foreach ($this->existingBackups as $backupInfo) : ?>
                    <tr>
                        <td style="text-align: center">
                            <input type="checkbox" class="selection" value="<?php echo basename($backupInfo->filename); ?>" style="text-align: center">
                        </td>
                        <td class="textright pre">
                            <?php if ($backupInfo->creationDateTime instanceof \DateTime) : ?>
                                <?php echo $backupInfo->creationDateTime->format('Y-m-d H:i:s'); ?>
                            <?php else : ?>
                                &mdash;
                            <?php endif; ?>
                        </td>
                        <td><?php echo $this->htmlEncode(basename($backupInfo->filename)); ?></td>
                        <td class="pre textright"><?php echo Util::sizeHumanReadable($backupInfo->filesize); ?></td>
                        <td class="pre textright"><?php echo $this->htmlEncode($backupInfo->version); ?></td>
                    </tr>
                <?php endforeach; ?>
            <?php else : ?>
                <tr class="noresult">
                    <td colspan="6">Keine Backups vorhanden</td>
                </tr>
            <?php endif; ?>
        </tbody>
    </table>
<?php $this->includeUserTemplate('blocks/list-paging.phtml'); ?>